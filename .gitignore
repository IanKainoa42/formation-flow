# Xcode
build/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Swift Package Manager
.build/
Packages/
Package.pins
Package.resolved

# Misc
*.swp
*~
*.xcuserstate

# Fastlane
fastlane/AuthKey.json
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Agent artifact cleanup — mechanical enforcement (Jules committed test_auth.swift in PR #48; patch*.sh + patch_file*.py in PR #92)
*.orig
*.patch
patch*.diff
*.diff
test_auth.swift
# Root-level .sh and .py intermediates Jules creates (legitimate scripts live in fastlane/)
/patch*.sh
/patch_file*.py
/*.py

# Agent / tool artifacts
.jules/
AppStoreScreenshots/auto/
*.af

# Jules agent state — capital J variant (Jules creates both .Jules/ and .jules/)
.Jules/

# Gemini agent state
.gemini/

# Jules planning artifact (committed in PRs #118, #120 — see CLAUDE.md commit hygiene)
plan.md
