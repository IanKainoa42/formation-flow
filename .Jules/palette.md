Learning: Added missing .accessibilityHint() modifiers to icon-only transport controls in TransitionViews.swift. Mirroring the existing .help() text ensures that VoiceOver users receive the same contextual information as pointer users, perfectly aligning with the SwiftUI accessibility convention for interactive elements without text.
Learning: Added missing .accessibilityHint() and .help() modifiers to icon-only buttons in `FormationThumbnailStrip.swift` and `ProUpgradeSheet.swift`. Mirroring the existing `.help()` text with `.accessibilityHint()` ensures that VoiceOver users receive the same contextual information as pointer users. When dynamic states exist (like a loading purchase state), using an immediately invoked closure (`{ ... }()`) allows for clean, inline logic evaluation for these modifiers.
Action: Whenever using `.accessibilityLabel` on icon-only buttons, check if dynamic `.accessibilityHint()` and `.help()` modifiers should be added to map to different button states (e.g., restricted access, loading, error).
