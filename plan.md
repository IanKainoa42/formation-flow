1. Add `.help()` and `.accessibilityHint()` to the "Clear selection" buttons (using `xmark.circle.fill`) in `AthleteDetailPanel.swift`.
2. Ensure the custom `MultiSelectionInspectorView` clear selection button in `AthleteDetailPanel.swift` has `.help()`.
3. Add `.help()` to the "Delete waypoint" button in `AthleteDetailPanel.swift`.
4. Add `.help()` to the upgrade `lock.fill` button in `AthleteDetailPanel.swift` (currently just has `.accessibilityLabel`).
5. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
6. Submit the pull request.
